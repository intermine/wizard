{
  "name": "wizard",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "build": "npx webpack --config webpack.config.js",
    "watchBuild": "npx webpack --watch --config webpack.config.js",
    "refreshServer": "npx nodemon ./bin/www",
    "dev": "concurrently \"npm run watchBuild\" \"npm run refreshServer\"",
    "cypress:open": "npx wait-on http://localhost:9992 && npx cypress open",
    "docker-compose": "docker-compose up --build --force-recreate",
    "cypress:test": "npm run docker-compose & npm run cypress:open",
    "pretest": "npx eslint --fix --ignore-path .gitignore ."
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "ejs": "~2.6.1",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "less-middleware": "~2.2.1",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "concurrently": "^4.1.1",
    "cypress": "^3.4.1",
    "eslint": "^6.3.0",
    "nodemon": "^1.19.1",
    "wait-on": "^3.3.0",
    "webpack": "^4.38.0",
    "webpack-cli": "^3.3.6"
  }
}
